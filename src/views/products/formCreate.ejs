<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/styles-header.css">
    <link rel="stylesheet" href="/css/styles-footer.css">

    <link rel="stylesheet" href="/css/styles-formCreate.css">

    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">

    <title>Coffe & Books</title>
</head>

<body>
    <%- include('../partials/mainHeader.ejs') %>

        <main class="create">
            <section class="add">

                <form action="/products/" method="post" enctype="multipart/form-data">

                    <h1>Añadir producto</h1>

                    <div class="inputs">
                        <div class="inputs-top">
                            <div class="left-side">
                                <div class="input">
                                    <label for="titulo">Titulo</label>
                                    <input type="text" name="titulo" id="titulo"
                                        value="<%= locals.old && old.titulo ? old.titulo: '' %>">
                                    <% if (locals.errors && errors.titulo) { %>
                                        <div class="feedback">
                                            <%= errors.titulo.msg %>
                                        </div>
                                        <% } %>
                                </div>

                                <div class="input">
                                    <label for="descripcion">Descripcion</label>
                                    <input type="text" name="descripcion" id="descripcion"
                                        value="<%= locals.old && old.descripcion ? old.descripcion: '' %>">
                                    <% if (locals.errors && errors.descripcion) { %>
                                        <div class="feedback">
                                            <%= errors.descripcion.msg %>
                                        </div>
                                        <% } %>
                                </div>

                            </div>

                            <div class="right-side">

                                <div class="input">
                                    <label for="autor">Autor</label>
                                    <input type="text" name="autor" id="autor"
                                        value="<%= locals.old && old.autor ? old.autor: '' %>">
                                    <% if (locals.errors && errors.autor) { %>
                                        <div class="feedback">
                                            <%= errors.autor.msg %>
                                        </div>
                                        <% } %>
                                </div>

                                <div class="input">
                                    <label for="categoria">Categoria</label>
                                    <select name="categoria" id="categoria"
                                        value="<%= locals.old && old.categoria ? old.categoria: '' %>">
                                        <option value="">Seleccione una opción</option>
                                        <option value="masVendidos" <%=locals.old && old.categoria==='masVendidos'
                                            ? 'selected' : '' ; %>>Mas vendidos</option>
                                        <option value="ofertas" <%=locals.old && old.categoria==='ofertas' ? 'selected'
                                            : '' ; %>>Ofertas</option>
                                        <option value="interes" <%=locals.old && old.categoria==='interes' ? 'selected'
                                            : '' ; %>>Interes</option>
                                    </select>
                                    <% if (locals.errors && errors.categoria) { %>
                                        <div class="feedback">
                                            <%= errors.categoria.msg %>
                                        </div>
                                        <% } %>
                                </div>

                            </div>
                        </div>
                        <div class="input">
                            <label for="genero">Genero/s</label>
                            <input type="text" name="genero" id="genero"
                                value="<%= locals.old && old.genero ? old.genero: '' %>">
                            <% if (locals.errors && errors.genero) { %>
                                <div class="feedback">
                                    <%= errors.genero.msg %>
                                </div>
                                <% } %>
                        </div>

                        <div class="inputs-numbers">

                            <div class="input-number">
                                <label for="cantidad">Cantidad</label>
                                <input type="number" name="cantidad" id="cantidad"
                                    value="<%= locals.old && old.cantidad ? old.cantidad: '1' %>">
                            </div>

                            <div class="input-number">
                                <label for="descuento">Descuento</label>
                                <input type="number" name="descuento" id="descuento"
                                    value="<%= locals.old && old.descuento ? old.descuento: '0' %>">
                            </div>

                            <div class="input-number">
                                <label for="precio">Precio</label>
                                <input type="number" name="precio" id="precio"
                                    value="<%= locals.old && old.precio ? old.precio: '0' %>">
                            </div>

                        </div>
                    </div>
                    <div class="input-file">
                        <label for="img">Imagen del producto</label>
                        <input type="file" name="img" id="img" value="<%= locals.old && old.img ? old.img: '' %>">
                        <% if (locals.errors && errors.img) { %>
                            <div class="feedback">
                                <%= errors.img.msg %>
                            </div>
                            <% } %>
                    </div>

                    <div class="buttons">
                        <button type="submit" class="btn-submit" title="Añadir"> Añadir libro </button>
                        <button type="reset" class="btn-reset" title="Añadir"> Limpiar </button>
                    </div>

                </form>

            </section>



        </main>
        <%- include('../partials/mainFooter.ejs') %>
</body>

</html>